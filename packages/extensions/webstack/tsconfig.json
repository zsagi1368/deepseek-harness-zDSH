{
  // Split-face package solution (mirrors packages/api/remotes): the host face
  // excludes src/client so the host aggregate never reaches the client
  // dependency chain, and the client face carries the React JSX sources.
  "files": [],
  "references": [
    {
      "path": "./tsconfig.host.json"
    },
    {
      "path": "./tsconfig.client.json"
    }
  ]
}
