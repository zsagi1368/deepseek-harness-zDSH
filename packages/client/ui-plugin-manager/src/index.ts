/** Host loader entry for the governance-tab browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the plugin manager tab. */
export function apply(): void {}
